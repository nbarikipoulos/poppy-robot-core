'use strict'

const PoppyRequestHandler = require('./PoppyRequestHandler')
const { getConfig, DEFAULT_SETTINGS } = require('./settings')

module.exports = {
  PoppyRequestHandler,
  getConfig,
  DEFAULT_SETTINGS
}
